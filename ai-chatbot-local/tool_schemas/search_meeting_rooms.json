{
  "name": "search_meeting_rooms",
  "description": "Search for available meeting rooms based on their capacity, features, and availability on a specific date/time. Use this when the user asks for a room for a specific number of people (capacity) or with specific amenities.",
  "parameters": {
    "type": "object",
    "properties": {
      "date": {
        "type": "string",
        "description": "Date for the booking in YYYY-MM-DD format, e.g. '2026-02-14'"
      },
      "start_time": {
        "type": "string",
        "description": "Start time in HH:MM format (24-hour), e.g. '14:00'"
      },
      "end_time": {
        "type": "string",
        "description": "End time in HH:MM format (24-hour), e.g. '15:00'"
      },
      "capacity": {
        "type": "integer",
        "description": "Optional. Minimum capacity required, e.g., 4 for 'find me a room for 4 people'"
      },
      "features": {
        "type": "string",
        "description": "Optional. Required amenities separated by comma, e.g. 'whiteboard,projector'"
      }
    },
    "required": [
      "date",
      "start_time",
      "end_time"
    ]
  }
}